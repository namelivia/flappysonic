import Instructions from './Instructions'
import {
	Bitmap,
	mockedBitmapConstructor,
} from 'createjs'

test('instructions are rendered when initialized', () => {
  //spriteSheetConstructorMock.mockImplementation(() => 'spriteSheet') TODO I havent been able to do this
  //const instructions = new Instructions('image')
});

//TODO: There are tests missing here!
